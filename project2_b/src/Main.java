import Store.Staff.*;
import Store.*;
import Store.Store;
import java.util.ArrayList;

public class Main {
    public static void main (String[] args) {
        Store store = new Store();
        store.run(30);
    }
}
